{
  "framework": "nextjs",
  "buildCommand": "prisma generate && node scripts/prod-db-setup.mjs && next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/update-tools",
      "schedule": "0 6 * * *"
    }
  ]
}
